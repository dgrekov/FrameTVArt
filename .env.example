# Example environment file for Frame TV Art Updater

# Required: IP address of your Samsung Frame TV
TV_IP=192.168.1.50

# Optional: Directory containing images to display (default: /art)
ART_FOLDER=/art

# Optional: Slideshow update period in minutes (0 = disabled, default: 0)
# Examples: 1440 = daily, 60 = hourly, 1 = every minute
UPDATE_INTERVAL=0

# Optional: How often to check folder for changes in seconds (0 = run once then exit, default: 60)
CHECK_INTERVAL=60

# Optional: Matte style for uploaded art (default: none)
# Valid values: none, shadowbox_polar, modern_apricot, etc. (see upstream matte list)
MATTE=none

# Optional: Token file path stored in persistent volume (default: /data/token_file.txt)
# Mount host ./data to container /data to persist this file
TOKEN_FILE=/data/token_file.txt

# Optional: Include TV favourites in slideshow rotation (0 or 1, default: 0)
INCLUDE_FAVOURITES=0

# Optional: Sequential slideshow instead of random (0 or 1, default: 0)
SEQUENTIAL=0

# Optional: Exit if TV is off (0 or 1, default: 0)
EXIT_IF_OFF=0

# Optional: Disable automatic sync using PIL (0 or 1, default: 1 = enabled)
SYNC=1

# Optional: Enable debug logging (0 or 1, default: 0)
DEBUG=0
